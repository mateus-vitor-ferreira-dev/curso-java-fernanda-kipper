# ==========================================================
# Ambiente de TESTES (profile "test", ativado por @ActiveProfiles("test"))
# Usa o mesmo PostgreSQL do Docker local (localhost:5434).
# Requer o container eventostec_postgres rodando durante os testes.
# ==========================================================
spring.datasource.url=jdbc:postgresql://localhost:5434/eventostec
spring.datasource.username=postgres
spring.datasource.password=postgres
